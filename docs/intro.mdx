---
sidebar_position: 1
---

# Intro

:::warning Work in Progress

DELETE THESE TEMPLATE TUTORIALS AND REPLACE THEM WITH ACTUAL CONTENT

:::

## Demo

```nix title="flake.nix"
{
  inputs.nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
  outputs = inputs: {

  };
}
```

:::tip Inspect this flake
Use <code>nix flake show</code> to inspect the output of this flake.
:::